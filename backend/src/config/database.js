// database.js

// Database configuration
const databaseConfig = {
  host: 'localhost',
  port: '5432',
  username: 'postgres',
  password: 'password',
  database: 'cash_saver_db',
};

module.exports = databaseConfig;
```